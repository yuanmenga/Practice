{
  class User {
    // public name: string; //在Ts中可以将这一步骤省略
    //public age: number
    constructor(public name: string, public age: number) {
      this.name = this.(name);
      this.age = age;
    }
  }

}
